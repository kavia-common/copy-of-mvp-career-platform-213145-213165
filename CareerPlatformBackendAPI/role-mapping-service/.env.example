# Role Mapping Service configuration

# Port to run the Express server on
PORT=4000

# Directory that contains JSON source data. Relative paths are resolved robustly.
# Default: data/imports (the service code will also attempt common fallbacks)
JSON_DIR=data/imports

# Optional: Enable verbose logging (true/false)
LOG_VERBOSE=false
