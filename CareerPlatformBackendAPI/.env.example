# Database configuration (optional for SQLite default)
# If DB_URL is not provided, the app defaults to sqlite:///./career_platform.db
DB_URL=
SQLITE_PATH=./career_platform.db

# Seed sample data on startup (optional)
# Set to 1/true/yes to insert a few sample roles if the roles table is empty.
SEED_SAMPLE_DATA=1
